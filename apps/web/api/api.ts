export const API = {
    api: (route: string) => route,
    feed: (route: string) => API.api(`/feed${route}`),
    feedLatest: () => API.feed("/latest"),
    feedSubscriptions: () => API.feed("/subscriptions"),
    // TODO feedPopular: () => API.feed('/popular'),
    user: (route: string) => API.api(`/user${route}`),
    media: (route: string) => API.api(`/media${route}`),
    mediaById: (id: string) => API.media(`/${id}`),
    verifyToken: () => API.user(`/verifyToken`),
    sessionLogin: () => API.user("/sessionLogin"),
    signOut: () => API.user("/sign-out"),
    currentUser: () => API.user("/current"),
    myJobs: () => API.user("/myJobs"),
    unobserveJob: () => API.user(`/unobserveJob`),
    mySubscriptions: () => API.user("/mySubscriptions"),
    subscribe: () => API.user("/subscribe"),
    unsubscribe: () => API.user("/unsubscribe"),
    rate: (videoId: string) => API.media(`/rate/${videoId}`),
    uploadProfilePicture: () => API.user("/uploadProfilePicture"),
    updateProfilePicture: () => API.user("/updateProfilePicture"),
    unsetProfilePicture: () => API.user("/unsetProfilePicture"),
    profilePictures: () => API.user("/profilePictures"),
    uploadMedia: () => API.media("/upload"),
    updateMedia: (id: string) => API.media(`/${id}`),
    myMedia: () => API.media("/my"),
    videoForEditing: (id: string) => API.media(`/edit/${id}`),

    youtube: (route: string) => API.api(`/youtube${route}`),
    youtubeUploadRandom: () => API.youtube("/upload-random"),
    testJob: () => API.api("/jobs/test"),
    searchUsers: () => API.user("/search"),
};
